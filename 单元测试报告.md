# 单元测试报告

> 依据:《软件工程基础实践-2026夏》任务书「把测试补齐并跑通」+《用例清单.md》(7 个业务用例,仓库根目录)
> 范围:全部 7 个业务用例(用例1 用户注册与登录 / 用例2 商品发布与编辑 / 用例3 商品购买 / 用例4 卖家发货 / 用例5 出价议价 / 用例6 售后处理 / 用例7 举报处理)+ 前端设施
> 日期:2026-08-27

---

## 一、测试结论

| 指标 | 结果 |
|---|---|
| **测试总数** | **139**(后端 115 + 前端 24) |
| **通过数** | **139** |
| **失败数** | **0** |
| **错误数** | 0 |
| **跳过数** | 0 |
| **失败原因** | 无 —— 本次全部用例通过,无失败、无错误、无跳过 |

后端 115 个 = 账户/商品/举报部分 32 个 + 交易履约部分 83 个。

- 账户/商品/举报部分执行结果:`Tests run: 32, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS`
- 交易履约部分执行结果:`Tests run: 83, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS`(2026-08-27 10:44 复验)
- 前端执行结果:`Test Files 5 passed (5)`,`Tests 24 passed (24)`

> 说明:当前目录已同步交易履约部分全部测试文件与账户部分的既有 `AuthServiceTest`(5 个),全量执行 `mvn test` 结果为 `Tests run: 88, Failures: 0, Errors: 0, Skipped: 0 — BUILD SUCCESS`;账户/商品/举报部分的新增测试文件(32 个中的 27 个)尚未同步到本目录,上列 32 个结果引自该部分测试报告,合并前请确认文件已全部提交。

---

## 二、运行环境

| 项 | 值 |
|---|---|
| 操作系统 | Windows 11(Home China,10.0.26200) |
| 后端构建工具 | Maven wrapper `mvnw.cmd`(3.9.8) |
| JDK | 本机验证:账户/商品/举报部分 21.0.11、交易履约部分 25.0.3(均 Eclipse Adoptium / Temurin);pom 目标 `java.version=17`;CI(GitHub Actions)使用 Temurin 17 |
| 后端框架 | Spring Boot 3.3.2 |
| 后端测试框架 | JUnit 5 (Jupiter) + Mockito 5.11(`spring-boot-starter-test`);byte-buddy / byte-buddy-agent 显式升级 1.17.7(test scope,兼容 JDK 25) |
| 前端运行时 | Node v24.16.0 / npm 11.13.0 |
| 前端框架 | Vue 3.5 + Vite 8 |
| 前端测试框架 | Vitest 4.1.8 + @vue/test-utils 2.4 + jsdom 29 + Pinia 3 |
| 数据库 | 不依赖 —— 纯 Mockito 单元测试,不启动 Spring 上下文,repository 全部 mock |
| 后端测试执行命令 | `cd backend && mvn test` |
| 前端测试执行命令 | `cd frontend && npm test`(= `vitest run`) |

---

## 三、后端测试明细

### 用例1 用户注册与登录

### 1. `AuthServiceTest.java`(扩展已有文件,`com.secondhand.auth.service`)— 12 个用例

**register(注册)— 5 个**

| 用例 | 断言要点 |
|---|---|
| 使用新手机号注册成功返回 token(已有) | token 非空、`verify(identityRepo).save(...)` |
| 重复身份注册抛 409(已有) | `IDENTITY_EXISTS` + `CONFLICT` |
| 邮箱标识统一转小写(新增) | `verify(exists(EMAIL, "foo@bar.com"))` + 保存的 identifier 为小写 |
| 手机号去除首尾空白(新增) | `verify(exists(PHONE, "13800138000"))` |
| 系统无管理员时首个注册者自动为 ADMIN(新增) | `result.role() == "ADMIN"` + `verify(save(user.role==ADMIN))` |

**login(登录)— 4 个**

| 用例 | 断言要点 |
|---|---|
| 正确密码登录返回 token(已有) | token/nickname 断言 |
| 错误密码抛 401(已有) | `UNAUTHORIZED` |
| 禁用账号抛 403(已有) | `FORBIDDEN` |
| 账号不存在抛 401(新增) | `INVALID_CREDENTIALS` + `UNAUTHORIZED` |

**changePassword(改密)— 3 个**

| 用例 | 断言要点 |
|---|---|
| 旧密码正确更新哈希(新增) | `verify(encode("newPass"))` + `verify(save(hash=="$2a$new"))` |
| 旧密码错误拒绝(新增) | `INVALID_CREDENTIALS` + `UNAUTHORIZED` |
| 用户不存在抛 404(新增) | `NOT_FOUND` |

---
### 2. `JwtServiceImplTest.java`(新建,`com.secondhand.auth.security`)— 4 个用例

| 用例 | 断言要点 |
|---|---|
| 签发 token 可解析 userId | `assertEquals(42L, parseUserId(token))` |
| 签发 token 可解析 role | `assertEquals("ADMIN", parseRole(token))` |
| 过期 token 解析抛异常 | `assertThrows(JwtException)`(负过期分钟构造) |
| 篡改/错误密钥解析抛异常 | `assertThrows(JwtException)`(不同密钥) |

---
### 用例2 商品发布与编辑

### 3. `ProductServiceTest.java`(新建,`com.secondhand.product.service`)— 10 个用例

**create(发布)— 3 个**

| 用例 | 断言要点 |
|---|---|
| 发布成功默认在售 | `status == ON_SALE` + `verify(save)` |
| 库存空/非正数默认 1 | `quantity == 1` |
| 免邮时邮费强制 0 | `shippingFeeCent == 0` |

**update(编辑)— 5 个**

| 用例 | 断言要点 |
|---|---|
| 商品不存在抛 404 | `NOT_FOUND` |
| 非卖家编辑抛 403 | `FORBIDDEN` |
| 售罄(库存0)上架抛 409 | `CONFLICT` |
| 卖家本人可编辑且仅更新非空字段 | 更新字段生效、未更新字段不变、`verify(save)` |
| 免邮切换邮费强制 0 | `shippingFeeCent == 0` |

**getById / listOnSale(浏览)— 2 个**

| 用例 | 断言要点 |
|---|---|
| 详情不存在抛 404 | `NOT_FOUND` |
| 列表只查在售且 size 上限 50 | `verify(findByStatusAndQuantityGreaterThan(ON_SALE, 0, ...))` + `pageSize==50` |

---
### 用例7 举报处理

### 4. `ReportServiceTest.java`(新建,`com.secondhand.report.service`)— 6 个用例

**submit(提交举报)— 2 个**

| 用例 | 断言要点 |
|---|---|
| 不能举报自己的商品抛 403 | `FORBIDDEN` + `verify(save, never())` |
| 举报成功置为 PENDING 并保存 | `status==PENDING` + reporterId/productId/reasonType 断言 |

**handle(处理)— 2 个**

| 用例 | 断言要点 |
|---|---|
| 举报不存在抛 404 | `NOT_FOUND` |
| 处理成功置 HANDLED + 记录信息 | `status==HANDLED` + handledBy/handledAt/note |

**dismiss(驳回)— 2 个**

| 用例 | 断言要点 |
|---|---|
| 举报不存在抛 404 | `NOT_FOUND` |
| 驳回成功置 DISMISSED + 记录信息 | `status==DISMISSED` + handledBy/handledAt/note |

---
### 用例3 商品购买(下单+支付)

### 5. `OrderServiceTest.java`(用例3 商品购买(20)+ 用例4 卖家发货(4))— 24 个用例

**用例3 下单 createOrder — 5 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC03-01` 以原价下单成功 | 库存减一、订单待支付、记录事件 |
| `UNIT-TC03-02` 库存最后一件售出后商品自动下架 | 状态流转与协作断言 |
| `UNIT-TC03-03` 不能购买自己的商品 | 抛出 FORBIDDEN |
| `UNIT-TC03-04` 非在售商品不能下单 | 抛出 CONFLICT |
| `UNIT-TC03-05` 已售罄商品不能下单 | 抛出 CONFLICT |

**用例3 支付 pay — 4 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC03-06` 支付成功 | 订单进入待发货,记录支付事件 |
| `UNIT-TC03-07` 收货信息不完整时支付被拒绝 | 抛出 BAD_REQUEST |
| `UNIT-TC03-08` 已支付订单重复支付被拒绝 | 抛出 CONFLICT |
| `UNIT-TC03-09` 非买家支付订单被拒绝 | 抛出 NOT_FOUND |

**用例3 取消 cancel — 2 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC03-10` 取消待支付订单 | 库存恢复并自动重新上架 |
| `UNIT-TC03-11` 非待支付状态订单不能取消 | 抛出 CONFLICT |

**用例4 发货 ship — 4 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC04-01` 卖家对已支付订单发货 | 运单创建、订单进入待收货 |
| `UNIT-TC04-02` 非待发货状态订单不能发货 | 抛出 CONFLICT |
| `UNIT-TC04-03` 非卖家不能发货 | 抛出 NOT_FOUND |
| `UNIT-TC04-04` 重复发货时更新已有运单 | 状态流转与协作断言 |

**用例3 确认收货与结算 — 4 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC03-12` 确认收货成功 | 订单完成,资金进入平台托管 |
| `UNIT-TC03-13` 非待收货状态不能确认收货 | 抛出 CONFLICT |
| `UNIT-TC03-14` 售后期满自动结算 | COMPLETED → SETTLED |
| `UNIT-TC03-15` 非完成状态订单不能结算 | 抛出 CONFLICT |

**收货信息与订单详情 — 4 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC03-16` 非买家不能修改收货信息 | 抛出 FORBIDDEN |
| `UNIT-TC03-17` 无关用户查看订单详情被拒绝 | 抛出 FORBIDDEN |
| `UNIT-TC03-18` 待支付订单详情 | 买家可支付、可取消,资金未托管 |
| `UNIT-TC03-19` 完成7天内的订单详情 | 买家可发起售后,资金托管中 |

**卖家已售商品 — 1 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC03-20` 卖家已售商品列表 | 含商品信息,无评分时评分为空 |

---
### 6. `MockPaymentServiceTest.java`(用例3 支付(Mock 实现))— 7 个用例

**创建与查询 — 2 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC03-21` 创建支付单 | 返回唯一支付号与待支付状态 |
| `UNIT-TC03-22` 查询不存在的支付单 | 返回已关闭 |

**支付回调 — 2 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC03-23` 支付成功回调后 | 支付单状态变为已支付 |
| `UNIT-TC03-24` 非成功回调不改变支付状态 | 状态流转与协作断言 |

**退款 — 3 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC03-25` 退款已存在的支付单 | 返回退款号与已退款状态 |
| `UNIT-TC03-26` 退款不存在的支付单 | 返回已关闭且无退款号 |
| `UNIT-TC03-27` mockPay 将支付单直接标记为已支付 | 状态流转与协作断言 |

---
### 用例4 卖家发货

### 7. `LogisticsServiceTest.java`(用例4 物流查询)— 3 个用例

**用例4 物流查询 trackByOrderId — 3 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC04-05` 订单不存在 | 抛出 NOT_FOUND |
| `UNIT-TC04-06` 订单无物流信息 | 抛出 NOT_FOUND |
| `UNIT-TC04-07` 正常返回物流轨迹 | 以运单号调用物流服务商 |

---
### 8. `MockLogisticsProviderTest.java`(用例4 Mock 物流服务商)— 1 个用例

**顶层 — 1 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC04-08` 返回四段式物流轨迹 | 揽收→在途→派送→签收 |

---
### 用例5 出价议价

### 9. `OfferServiceTest.java`(用例5 出价议价)— 12 个用例

**用例5 发起报价 createOffer — 4 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC05-01` 买家成功发起报价 | 状态为待卖家回复 |
| `UNIT-TC05-02` 不能给自己的商品报价 | 抛出 FORBIDDEN |
| `UNIT-TC05-03` 已售罄商品不能报价 | 抛出 CONFLICT |
| `UNIT-TC05-04` 无效报价金额被拒绝 | 抛出 BAD_REQUEST |

**用例5 卖家接受报价 acceptOffer — 3 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC05-05` 接受报价 | 报价标记已接受,按报价价格创建订单 |
| `UNIT-TC05-06` 非卖家不能接受报价 | 抛出 FORBIDDEN |
| `UNIT-TC05-07` 非待处理报价不能接受 | 抛出 CONFLICT |

**用例5 卖家拒绝报价 rejectOffer — 2 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC05-08` 卖家拒绝报价 | 状态为已拒绝 |
| `UNIT-TC05-09` 非待处理报价不能拒绝 | 抛出 CONFLICT |

**用例5 买家取消报价 cancelOffer — 3 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC05-10` 买家取消报价 | 状态为已取消 |
| `UNIT-TC05-11` 非买家不能取消报价 | 抛出 FORBIDDEN |
| `UNIT-TC05-12` 非待处理报价不能取消 | 抛出 CONFLICT |

---
### 用例6 售后处理(申请+审批+仲裁)

### 10. `AfterSaleServiceTest.java`(用例6 售后处理(申请+审批+仲裁))— 36 个用例

**用例6 发起售后申请 request — 7 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC06-01` 确认收货7天内发起售后成功 | 进入待卖家审核,72h 处理时效 |
| `UNIT-TC06-02` 非买家不能发起售后 | 抛出 FORBIDDEN |
| `UNIT-TC06-03` 确认收货前不能手动发起售后 | 抛出 FORBIDDEN |
| `UNIT-TC06-04` 超过7天售后窗口期发起售后被拒绝 | 抛出 GONE |
| `UNIT-TC06-05` 已有进行中售后时不能重复发起 | 抛出 CONFLICT |
| `UNIT-TC06-06` 退款金额超过订单金额时按订单金额封顶 | 状态流转与协作断言 |
| `UNIT-TC06-07` 发起售后将订单标记为售后处理中 | 状态流转与协作断言 |

**用例6 卖家审批 approve/reject — 5 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC06-08` 同意仅退款 | 直接退款,订单全额退款后作废 |
| `UNIT-TC06-09` 同意退货退款 | 进入待买家寄件,7天寄件时效 |
| `UNIT-TC06-10` 非卖家不能审批售后 | 抛出 FORBIDDEN |
| `UNIT-TC06-11` 已完结售后不能重复审批 | 抛出 CONFLICT |
| `UNIT-TC06-12` 卖家拒绝售后 | 记录拒绝理由,买家3天内可申请平台介入 |

**用例6 退货寄回与收货确认 — 6 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC06-13` 买家寄回退货 | 进入待卖家收货,10天确认时效 |
| `UNIT-TC06-14` 非待寄件状态不能寄回 | 抛出 CONFLICT |
| `UNIT-TC06-15` 仅退货退款类型需要寄回 | 抛出 CONFLICT |
| `UNIT-TC06-16` 卖家确认收货 | 退款并作废订单 |
| `UNIT-TC06-17` 非待收货状态不能确认收货 | 抛出 CONFLICT |
| `UNIT-TC06-18` 卖家拒绝收货 | 售后被拒绝,买家可申请平台介入 |

**用例6 平台仲裁 — 9 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC06-19` 卖家拒绝后买家申请平台介入 | 5天仲裁时效 |
| `UNIT-TC06-20` 非拒绝状态不能申请平台介入 | 抛出 CONFLICT |
| `UNIT-TC06-21` 仲裁全额退款 | 退款并作废订单 |
| `UNIT-TC06-22` 仲裁部分退款 | 退款但订单保持已完成(买家留用) |
| `UNIT-TC06-23` 仲裁驳回 | 关闭售后并按进度恢复订单状态 |
| `UNIT-TC06-24` 仲裁退货退款 | 进入待买家寄件 |
| `UNIT-TC06-25` 仲裁结果含责任判定与运费归属 | 状态流转与协作断言 |
| `UNIT-TC06-26` 无效仲裁结果被拒绝 | 抛出 BAD_REQUEST |
| `UNIT-TC06-27` 未进入仲裁状态不能仲裁 | 抛出 CONFLICT |

**用例6 超时自动处理 processTimeouts — 7 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC06-28` 卖家72h超时未处理仅退款 | 自动同意并退款 |
| `UNIT-TC06-29` 卖家72h超时未处理退货退款 | 自动同意进入待寄件 |
| `UNIT-TC06-30` 买家3天未申请平台介入 | 售后关闭并恢复订单 |
| `UNIT-TC06-31` 买家7天未寄件 | 售后关闭并恢复订单 |
| `UNIT-TC06-32` 卖家10天未确认收货 | 自动退款并作废订单 |
| `UNIT-TC06-33` 待收货14天超时 | 系统自动发起仅退款售后 |
| `UNIT-TC06-34` 已有进行中售后的超时订单跳过 | 不重复发起 |

**用例6 买家取消售后 — 2 个**
| 用例 | 断言要点 |
|---|---|
| `UNIT-TC06-35` 买家取消售后 | 关闭并按进度恢复订单状态(已发货→待收货) |
| `UNIT-TC06-36` 已完结售后不能取消 | 抛出 CONFLICT |

---
## 四、前端测试明细(`frontend/src/__tests__/`)

### 1. `api.test.js`(新建)— 7 个用例
- 自动注入 `Authorization: Bearer <token>` 头
- 无 token 时不注入 Authorization
- `auth=false` 时即使有 token 也不注入
- 成功时解包返回 `data`
- `success=false` 时抛出后端错误信息
- body 对象自动 JSON 序列化
- raw 模式不设 Content-Type 且 body 原样传递

### 2. `userStore.test.js`(新建)— 5 个用例
- `setAuth` 写入状态与 localStorage
- `clearAuth` 清空状态与 localStorage
- `fetchProfile` 成功更新昵称/角色/userId
- `fetchProfile` 失败清除登录态
- 无 token 时 `fetchProfile` 直接返回不请求

### 3. `notificationStore.test.js`(新建)— 5 个用例
- `fetch` 设置各计数
- `fetch` 异常时计数保持不变
- `pendingTotal` 为各待处理订单之和
- `startPolling` 立即拉取并按间隔轮询(fake timers)
- `stopPolling` 停止轮询

### 4. `toast.test.js`(新建)— 2 个用例
- `show` 调用注册的回调
- `success/error/warn` 传递对应类型

### 5. `AppIcon.test.js`(已有)— 5 个用例(保持原样,纳入统计)

---

## 五、未实现规则说明(文档要求 vs 代码实际)

以下规则在《单元测试分工表》中列出,但**当前代码并未实现**,本次按约定「只测已实现逻辑」,未为其编写测试(未写"测不存在行为"的假测试)。如需覆盖,需先补齐主代码实现:

| # | 文档要求 | 代码实际 | 涉及文件 |
|---|---|---|---|
| 1 | 用例1:手机号/邮箱格式校验失败 | 服务层无格式校验,仅 `@NotBlank`/`@Size`,格式校验在 Controller `@Valid`(bean validation) | `AuthService` / `RegisterRequest` |
| 2 | 前端:api.js「401 清除登录态」 | `api.js` 无 401 处理;仅 `userStore.fetchProfile` 的 catch 中会 `clearAuth()` | `api.js` |
| 3 | 用例2:标题/价格/成色字段校验失败 | `ProductService.create` 无校验,校验在 Controller `@Valid CreateProductRequest` | `ProductService` |
| 4 | 用例2:已售/已下架商品不可编辑 | `update()` 仅有「售罄(库存0)无法上架」,无「已下架不可编辑」判断 | `ProductService` |
| 5 | 用例7:重复举报拒绝 | `ReportService.submit` 无重复举报检查 | `ReportService` |
| 6 | 用例7:PENDING→HANDLED(下架商品) | `handle()` 仅置状态 HANDLED,无「下架商品」动作 | `ReportService` |
| 7 | 用例7:PENDING→DISMISSED(附原因,空原因拒绝) | `dismiss()` 无空原因校验 | `ReportService` |
| 8 | 用例5:出价低于底价/低于当前最高价拒绝 | `createOffer` 仅校验金额 > 0,无底价与当前最高价比较 | `OfferService` |
| 9 | 用例5:商品下架后不能出价 | `createOffer` 仅校验库存,未校验商品状态 | `OfferService` |
| 10 | 用例6:卖家拒绝售后必须附理由(空理由拒绝) | `reject`/`rejectReturn` 未校验理由是否为空 | `AfterSaleService` |
| 11 | 用例4:物流 provider 异常降级 | `trackByOrderId` 未捕获 provider 异常 | `LogisticsService` |
| 12 | 用例4:发货运单号/快递公司格式校验 | `ship()` 未校验 `carrierCode`/`trackingNo` | `OrderService` |
| 13 | 用例3:重复下单拦截 | `createOrder` 无重复下单检查 | `OrderService` |

---

## 六、测试文件位置

**后端**(`backend/src/test/java/com/secondhand/`)

- `auth/service/AuthServiceTest.java`(扩展,12 个用例)
- `auth/security/JwtServiceImplTest.java`(新建,4 个用例)
- `product/service/ProductServiceTest.java`(新建,10 个用例)
- `report/service/ReportServiceTest.java`(新建,6 个用例)
- `order/service/OrderServiceTest.java`(新建,24 个用例)
- `payment/MockPaymentServiceTest.java`(新建,7 个用例)
- `logistics/service/LogisticsServiceTest.java`(新建,3 个用例)
- `logistics/provider/MockLogisticsProviderTest.java`(新建,1 个用例)
- `offer/service/OfferServiceTest.java`(新建,12 个用例)
- `aftersale/service/AfterSaleServiceTest.java`(新建,36 个用例)
- `testutil/TestId.java`(新建,实体 id 反射注入工具)

**前端**(`frontend/src/__tests__/`)

- `api.test.js`、`userStore.test.js`、`notificationStore.test.js`、`toast.test.js`(新建)
- `AppIcon.test.js`(已有)

**配套交付物**

- 《单元测试编号登记表.md》(交易履约部分 83 行 UNIT-TC 编号,供追溯表合并;账户/商品/举报部分沿用 UNIT-TC01~50 顺序段,见其测试代码)
- `backend/target/surefire-reports/`(Surefire 原始报告)
- `backend/pom.xml`(byte-buddy 1.17.7 升级,见本报告「二、运行环境」)

---

## 附录:编号说明

用例编号以《用例清单.md》(7 个用例)为准。交易履约部分(用例3~6)测试编号采用 `UNIT-TC<用例号>-<序号>` 锚定,83 个编号见《单元测试编号登记表.md》;账户/商品/举报部分(用例1/2/7)沿用 `UNIT-TC01~50` 顺序段,逐条对应关系以该部分测试代码的 `@DisplayName` 为准。

---
