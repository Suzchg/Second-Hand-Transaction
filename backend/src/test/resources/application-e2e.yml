spring:
  datasource:
    url: jdbc:mysql://localhost:3306/secondhand_test?characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 123000
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

app:
  security:
    jwt:
      secret: e2e-test-secret-key-do-not-use-in-production
      access-token-expiration-minutes: 60

logistics:
  kuaidi100:
    enabled: false
